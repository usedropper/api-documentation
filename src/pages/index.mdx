---
title: API Documentation
---

import clsx from 'clsx';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import Layout from '@theme/Layout';
import styles from './index.module.css';
import ThemedImage from '@theme/ThemedImage';
import useBaseUrl from '@docusaurus/useBaseUrl';

export const Tagline = () => <p style={{'font-size': '1.5em'}} >{useDocusaurusContext().siteConfig.tagline} 📦</p>

<ThemedImage
  style={{ display: 'block' }}
  width="250"
  alt="Logo"
  sources={{
    light: useBaseUrl('/img/logo.svg'),
    dark: useBaseUrl('/img/darkLogo.svg'),
  }}
/>

<Tagline/>

<br/>

:::tip
Before reading these docs and using the API, please ensure you have access to the [enterprise dashboard](https://dashboard.usedropper.com).
:::

<br/>
<br/>

export const Button = ({children, color}) => (
  <span
    style={{
      backgroundColor: '#2EBB55',
      borderRadius: '10px',
      color: '#fff',
      padding: '15px 40px',
      cursor: 'pointer',
    }}>
    {children}
  </span>
);

<a href="/docs/intro"><Button>Get Started 🚀</Button></a>