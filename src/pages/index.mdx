---
title: Markdown page example
---

import clsx from 'clsx';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import Layout from '@theme/Layout';
import styles from './index.module.css';

export const LocalTitle = () => <p>{useDocusaurusContext().siteConfig.title} </p>;

export const Tagline = () => <p style={{'font-size': '2em'}} >{useDocusaurusContext().siteConfig.tagline} 📦</p>;

<a href="/docs/intro"><img src="/img/logo.svg" width="250"/></a>

**<Tagline/>**

<br/>
<br/>
<br/>

:::tip
Before reading these docs and using the API, please ensure you have access to the [enterprise dasboard](https://dashboard.usedropper.com).
:::

<br/>
<br/>


<a href="/docs/intro"><Button>Get Started 🚀</Button></a>



export const Button = ({children, color}) => (
  <span
    style={{
      backgroundColor: '#2EBB55',
      borderRadius: '10px',
      color: '#fff',
      padding: '15px 40px',
      cursor: 'pointer',
    }}>
    {children}
  </span>
);